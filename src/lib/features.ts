/** Set true to show the Containment Protocol button and allow /containment visits. */
export const CONTAINMENT_GAME_PUBLIC = false;

/** Set true to overlay the public site with FACILITY LOCKDOWN emergency state. */
export const FACILITY_LOCKDOWN = false;

/** Set true to use the new cinematic homepage at `/`. Set false to restore Transmission. */
export const USE_NEW_HOMEPAGE = true;
